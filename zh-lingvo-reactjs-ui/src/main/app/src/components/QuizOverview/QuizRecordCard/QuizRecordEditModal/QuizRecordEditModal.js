import React from 'react';
import { useSelector } from 'react-redux';

const QuizRecordEditModal = props => {
    const showModal = useSelector(quizReco)

    return null;
};

export default QuizRecordEditModal;
