import React from 'react';

import classes from './Logo.module.scss';

const Logo = () => {
    return (
        <a href="#">
            <div className={classes.Logo} />
        </a>
    );
};

export default Logo;
